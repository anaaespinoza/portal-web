---
// Ruta "/investigacion/:unidad" — origen: <InvestUA /> (dinámica)
import BaseLayout from "../../layouts/BaseLayout.astro";

export function getStaticPaths() {
  return []; // TODO: poblar con unidades cuando el contenido esté portado
}
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    InvestUA — contenido pendiente de portar (ruta dinámica).
  </p>
</BaseLayout>