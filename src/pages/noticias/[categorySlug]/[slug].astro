---
// Ruta "/noticias/:categorySlug/:slug" — origen: <Noticia /> (alias, dinámica)
import BaseLayout from "../../../layouts/BaseLayout.astro";

export function getStaticPaths() {
  return []; // TODO: poblar con noticias cuando el contenido esté portado
}
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    Noticia (alias categoría) — contenido pendiente de portar (ruta dinámica).
  </p>
</BaseLayout>