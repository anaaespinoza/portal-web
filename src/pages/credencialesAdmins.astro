---
// Ruta "/credencialesAdmins" — origen: <PrivateRoute requiredPermission="credencialesAdmins"><Credenciales /></PrivateRoute>
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    Credenciales (privada) — contenido pendiente de portar.
  </p>
</BaseLayout>