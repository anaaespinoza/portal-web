---
// Ruta "/pasedirecto" — origen: <PaseDirecto /> (src/screens/pasedirecto)
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    PaseDirecto — contenido pendiente de portar.
  </p>
</BaseLayout>