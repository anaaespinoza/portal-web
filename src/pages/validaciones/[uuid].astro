---
// Ruta "/validaciones/:uuid" — origen: <Validaciones /> (dinámica)
import BaseLayout from "../../layouts/BaseLayout.astro";

export function getStaticPaths() {
  return []; // TODO: poblar cuando el contenido esté portado (validaciones por uuid)
}
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    Validaciones — contenido pendiente de portar (ruta dinámica).
  </p>
</BaseLayout>