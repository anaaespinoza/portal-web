---
// Ruta "/en-linea" — origen: <UnidadPagina slug="en-linea" /> (src/screens/unidad)
import BaseLayout from "../../layouts/BaseLayout.astro";
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    UnidadPagina (en-linea) — contenido pendiente de portar.
  </p>
</BaseLayout>