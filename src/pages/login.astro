---
// Ruta "/login" — origen: <Login /> (src/screens/login)
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    Login — contenido pendiente de portar.
  </p>
</BaseLayout>