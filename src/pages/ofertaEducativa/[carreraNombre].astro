---
// Ruta "/ofertaEducativa/:carreraNombre" — origen: <OfertaCarrera /> (dinámica)
import BaseLayout from "../../layouts/BaseLayout.astro";

export function getStaticPaths() {
  return []; // TODO: poblar con el listado de carreras cuando el contenido esté portado
}
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    OfertaCarrera — contenido pendiente de portar (ruta dinámica).
  </p>
</BaseLayout>