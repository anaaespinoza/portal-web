---
// Ruta "/ofertaEducativa/filtro/:idFilter" — origen: <OfertaEducativa /> (dinámica)
import BaseLayout from "../../../layouts/BaseLayout.astro";

export function getStaticPaths() {
  return []; // TODO: poblar con los filtros cuando el contenido esté portado
}
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    OfertaEducativa (filtro) — contenido pendiente de portar (ruta dinámica).
  </p>
</BaseLayout>