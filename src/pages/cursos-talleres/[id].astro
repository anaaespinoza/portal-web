---
// Ruta "/cursos-talleres/:id" — origen: <TallerDetalle /> (dinámica)
import BaseLayout from "../../layouts/BaseLayout.astro";

export function getStaticPaths() {
  return []; // TODO: poblar con cursos/talleres cuando el contenido esté portado
}
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    TallerDetalle — contenido pendiente de portar (ruta dinámica).
  </p>
</BaseLayout>