---
// Ruta "/validarCredencial/:qrId" — origen: <ValidadorCredenciales /> (dinámica)
import BaseLayout from "../../layouts/BaseLayout.astro";

export function getStaticPaths() {
  return []; // TODO: poblar cuando el contenido esté portado (validación por QR)
}
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    ValidadorCredenciales — contenido pendiente de portar (ruta dinámica).
  </p>
</BaseLayout>