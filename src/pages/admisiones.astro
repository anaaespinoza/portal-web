---
// Ruta "/admisiones" — origen: <Admisiones /> (src/screens/admisiones)
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    Admisiones — contenido pendiente de portar.
  </p>
</BaseLayout>