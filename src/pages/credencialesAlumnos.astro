---
// Ruta "/credencialesAlumnos" — origen: <PrivateRoute requiredPermission="credencialesAlumnos"><CredencialesAlumnos /></PrivateRoute>
import BaseLayout from "../layouts/BaseLayout.astro";
---
<BaseLayout>
  <p style="font-family:'madaniArabicLight',sans-serif;color:var(--textdark);padding:2rem">
    CredencialesAlumnos (privada) — contenido pendiente de portar.
  </p>
</BaseLayout>